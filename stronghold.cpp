#include "stronghold.h"
#include "stronghold_global.h"

/*
User* Stronghold::userJoin(const QString &user, const QString &passwd)
{
    QString dbfile = login->login(user, xsPassword(passwd));
    if(!dbfile.isEmpty())
    {
        database = new xsDatabase(dbfile, "passwd");
        blowfish = new xsBlowfish(passwd);
        return ;
    }
    return false;
}
*/
